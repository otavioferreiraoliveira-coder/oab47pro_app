name: oab47pro_app
description: OAB 47 PRO — Preparação Exame de Ordem
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  http: ^1.2.2
  fl_chart: ^0.69.0
  flutter_tts: ^4.0.2
  flutter_markdown: ^0.7.3
  intl: ^0.19.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/questoes.json
    - assets/data/meta.json
    - assets/data/plano.json
